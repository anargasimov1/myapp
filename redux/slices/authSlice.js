import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

const authSliceSlice = createSlice({
    name: "auth",
    initialState,
    reducers: {

    },
    extraReducers: (builder) => {
       
    }

});

export const { } = authSliceSlice.actions;
export default authSliceSlice.reducer;
